# Worklog 2026-04-10

## Scope

- Convert `parents` into real Claude Code slash commands under `.claude/commands/`
- Keep `/parent` and `/parent-no-opus` routing behavior intact
- Replace the synthetic one-shot demo with a real interactive Claude CLI capture
- Prepare bilingual repository documentation for GitHub publication

## Changes

### Command registration

- Added `.claude/commands/parent.md`
- Added `.claude/commands/parent-no-opus.md`
- Removed the earlier `.claude/skills/*` command wrappers

### Router

- Updated `scripts/parent.py` to allow environment overrides for transcript retry count and retry sleep
- Kept deterministic routing, no fallback, and plain Claude-style output

### Demo pipeline

- Replaced the old synthetic demo builder with a real pipeline:
  - `scripts/build_demo.py`
  - `scripts/capture_interactive_demo.py`
  - `scripts/render_interactive_demo.py`
- The capture step records a real `claude` TTY session through `script -m advanced`
- The render step replays the terminal stream and applies crop, pacing compression, and highlight treatment

### Documentation

- Rewrote `README.md` in English and Korean
- Updated installation guidance to `.claude/commands/`
- Updated demo instructions and asset inventory

## Demo Artifacts

- `assets/demo/parent-interactive.gif`
- `assets/demo/parent-interactive.mp4`
- `assets/demo/parent-interactive.txt`
- `assets/demo/source/parent-interactive.in`
- `assets/demo/source/parent-interactive.out`
- `assets/demo/source/parent-interactive.time`

## Validation

### Command validation

- `claude -p '/parent --dry-run rename one variable'`
  - Result: `I'll use Haiku in execute mode with low effort based on the request scope, risk, and ambiguity.`
- `claude -p '/parent-no-opus --dry-run Design a new authentication architecture with migration planning'`
  - Result: `I'll stay on Sonnet in plan mode with max effort because this command excludes Opus and the request still looks broad or risky.`

### Test validation

- `python3 -m py_compile scripts/parent.py scripts/build_demo.py scripts/capture_interactive_demo.py scripts/render_interactive_demo.py tests/test_parent_router.py tests/test_demo_renderer.py`
- `python3 -m unittest discover -s tests -v`

### Demo validation

- `python3 scripts/build_demo.py`
- Verified output files were regenerated
- Verified `assets/demo/parent-interactive.mp4` opens successfully

## Notes

- The published demo focuses on one clean `/parent` interactive run to keep the video readable
- `/parent-no-opus` remains validated by direct command execution and README examples

## Follow-up Investigation

### Duplicate command output in Claude UI

- Symptom:
  - The same response appeared once in the Bash tool block and once again as a final assistant bubble
- Root cause:
  - The command wrapper asked Claude to run Bash and then effectively echo the script output again
  - `scripts/parent.py` was already printing the final user-facing response to stdout
- Fix:
  - Updated `.claude/commands/parent.md`
  - Updated `.claude/commands/parent-no-opus.md`
  - Updated `scripts/install_global_commands.py`
  - Wrapper instructions now explicitly require no final assistant message after the Bash tool finishes

### Latency reduction analysis

- Baseline conclusion:
  - The dominant cost is architectural because `/parent` uses one Claude pass to interpret the slash command and a second Claude pass inside `scripts/parent.py`
- Biggest avoidable delay:
  - The wrapper was forcing `scripts/parent.py` to recover command arguments from the Claude transcript file
  - That path uses retry polling and can add noticeable latency or long worst-case stalls
- Optimization applied:
  - Updated both command wrappers and the global installer to pass `$ARGUMENTS` directly to `scripts/parent.py` through stdin
  - This preserves quoting safely via a here-doc and lets `load_request_text()` return immediately without transcript polling when arguments are present
- Remaining latency:
  - A second Claude invocation is still required by design, so `/parent` will remain slower than a normal direct Claude reply

# 작업 기록 2026-04-10-2

## 요약
`/parent`가 계획형 요청을 child Claude로 넘길 때 `--permission-mode plan` 대신 `--permission-mode auto`를 사용하도록 조정했다. 동시에 read-only 도구 제한은 그대로 유지해 계획 응답의 안전성은 보존했다.

## 배경
기존 실행 로그에서 계획형 요청이 `--permission-mode plan`으로 실행되는 것을 확인했다. 현재 라우터는 child 프롬프트에서 이미 "계획만 작성하라"고 강하게 제한하고 있으므로, 실행 권한 제어는 `auto`로 두고 도구 허용 목록만 `Read,Grep,Glob`으로 고정하는 편이 더 호환성이 높고 단순하다.

## 변경 사항
- `scripts/parent.py`
  - plan 경로의 child Claude 호출 인자를 `--permission-mode auto`로 변경했다.
  - `--tools Read,Grep,Glob` 및 `--allowed-tools Read,Grep,Glob` 제한은 유지했다.
- `tests/test_parent_router.py`
  - 계획형 라우트 테스트를 갱신해 `plan` permission mode를 기대하지 않고, `auto`와 read-only 도구 제한을 함께 검증하도록 했다.
- `docs/worklog-2026-04-10.md`
  - 이번 반복의 작업 기록을 한국어로 추가했다.

## 검증
- `python3 -m unittest discover -s tests -v`
  - 결과: 17개 테스트 통과.
- `python3 -m py_compile scripts/parent.py scripts/build_demo.py scripts/capture_interactive_demo.py scripts/render_interactive_demo.py tests/test_parent_router.py tests/test_demo_renderer.py`
  - 결과: 성공.
- `printf '%s' '--mode plan Plan a migration for the production auth system' | PARENTS_CLAUDE_BIN=/bin/echo python3 scripts/parent.py`
  - 결과: `-p --model opus --effort high --disable-slash-commands --no-session-persistence --permission-mode auto --tools Read,Grep,Glob --allowed-tools Read,Grep,Glob`
  - 확인: 계획형 요청에서도 실제 child argv에 `--permission-mode auto`가 사용되고, read-only 도구 제한은 유지된다.

## 남은 사항
- 실제 Claude CLI 환경에서 `auto` permission mode가 모든 버전에서 동일하게 동작하는지 추가 실사용 검증이 있으면 좋다.
- 다음 반복 후보는 `.parent/runs` 로그 요약에 child permission/tool 설정을 Markdown에도 더 명확히 노출하는 개선이다.

# 작업 기록 2026-04-10-3

## 요약
`.parent/runs` Markdown 요약에도 child Claude 실행의 permission mode와 tool 제한이 보이도록 로그 출력을 확장했다. 이제 사람이 JSON을 열지 않아도 계획형 라우트가 어떤 실행 제약으로 호출됐는지 바로 확인할 수 있다.

## 배경
이전 반복에서 계획형 요청의 `--permission-mode`를 `auto`로 조정했지만, 그 사실은 JSON 로그의 `child_argv_summary`에서만 확인할 수 있었다. 일상적인 확인 경로는 Markdown 요약이므로 같은 정보를 거기에도 노출하는 편이 디버깅과 감사에 더 직접적이다.

## 변경 사항
- `scripts/parent.py`
  - child argv에서 특정 플래그 값을 읽는 작은 helper를 추가했다.
  - Markdown 실행 요약에 `Child permission mode`와 `Child tools` 항목을 추가했다.
- `tests/test_parent_router.py`
  - 로그 파일을 실제로 생성한 뒤 Markdown 본문에 새 항목이 포함되는지 검증하는 테스트를 추가했다.
- `docs/parent-routing.md`
  - 로그 문서에 child permission mode와 tool 제한이 기록된다는 설명을 반영했다.

## 검증
- `python3 -m unittest discover -s tests -p 'test_parent_router.py' -v`
  - 결과: 14개 테스트 통과.
- `python3 -m py_compile scripts/parent.py tests/test_parent_router.py`
  - 결과: 성공.
- `python3 -c "import os, pathlib, subprocess, tempfile; ..."`
  - 임시 프로젝트 루트에서 `PARENTS_CLAUDE_BIN=/bin/echo`로 `scripts/parent.py`를 실행해 실제 Markdown 로그를 생성했다.
  - 생성된 로그에 `Child permission mode: auto`와 `Child tools: Read,Grep,Glob`가 포함되는 것을 확인했다.

## 남은 사항
- 현재 Markdown 요약에는 `--allowed-tools` 값이 별도 항목으로 노출되지는 않는다. 필요하면 다음 반복에서 추가할 수 있다.
- 다음 반복 후보는 `docs/parent-routing.md`의 8번 줄 설명처럼 stdin 우선 입력 경로를 더 명확히 문서화하는 작업이다.

# 작업 기록 2026-04-10-4

## 요약
문서에서 `/parent`와 `/parent-no-opus`가 항상 transcript에서만 인자를 복구하는 것처럼 보이던 설명을 수정했다. 이제 README와 라우팅 문서 모두 stdin 우선 입력과 transcript fallback 흐름을 정확히 설명한다.

## 배경
코드는 이미 wrapper가 인자를 stdin으로 넘겨주면 그 경로를 먼저 사용하고, 필요한 경우에만 transcript를 조회한다. 하지만 공개 문서에는 여전히 transcript-only 동작처럼 적혀 있어서 실제 구현과 설명이 어긋나 있었다.

## 변경 사항
- `README.md`
  - 영어/한국어 개요 문장을 모두 수정해 stdin 우선 + transcript fallback 동작을 반영했다.
- `docs/parent-routing.md`
  - 라우터 동작 설명을 같은 내용으로 맞췄다.
- `docs/worklog-2026-04-10.md`
  - 이번 문서 정리 반복을 한국어로 기록했다.

## 검증
- `grep -n "recover|stdin|transcript|복구" README.md docs/parent-routing.md scripts/parent.py`
  - 문서의 핵심 문구와 코드의 `load_request_text()` 구현을 대조해 stdin 우선 설명이 반영됐는지 확인했다.
- `read README.md`, `read docs/parent-routing.md`, `read scripts/parent.py`
  - 수정된 문장과 실제 코드 흐름이 일치하는지 직접 검토했다.

## 남은 사항
- README의 사용 예시 자체는 여전히 유효하지만, wrapper가 stdin 전달을 수행하는 방식까지 별도 섹션으로 설명할지는 추후 결정할 수 있다.
- 현재 시점에서 눈에 띄는 안전한 불일치는 정리했으므로, 다음 반복은 새 기능 추가보다 사용자 요구나 제품 방향이 생길 때 다시 여는 편이 적절하다.
